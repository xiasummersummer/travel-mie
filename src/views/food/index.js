// import { connect } from 'resact-redux';
// import UI from './UI';

// const Com = connect()(UI)

// export default Com;
import { connect } from 'react-redux';
import UI from './UI';
const Com = connect()(UI)

export default Com;

